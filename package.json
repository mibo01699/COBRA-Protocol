{
  "name": "cobra-protocol",
  "version": "1.0.0",
  "description": "COBRA Protocol: Crisis-Resilient Connectivity Infrastructure (Sandbox/Testnet)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "exit 0",
    "test": "jest --runInBand || exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}