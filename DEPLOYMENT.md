# 📡 دليل النشر والتشغيل المحلي لبروتوكول Cobra eSIM 🐍

يحتوي هذا الدليل على التعليمات الهندسية لتشغيل العقد البرمجي الخاص بـ **Cobra eSIM** محلياً على جهازك، مع تطبيق قيود الصفر العشري والربط اللحظي ببيئة **`BIGISH-YER`**.

## 🛠️ المتطلبات الأساسية للبيئة المحلية
* **Node.js** إصدار v18 أو أحدث.
* **Python** إصدار 3.10 أو أحدث.
* بيئة فحص العقود الذكية (**Hardhat** أو **Foundry**).

## 🚀 خطوات التشغيل والتهيئة (Step-by-Step)

### 1. تثبيت الحزم والاعتمادات البرمجية
قم بفتح الطرفية (Terminal) في مجلد المشروع الرئيسي ونفذ الأمر التالي:
```bash
npm install
```

### 2. إعداد ملف البيئة السري والمغلق
تأكد من وجود ملف `.env.production` في المجلد الرئيسي ومطابقته للعناوين والمحافظ التي تم ربطها بمنصة إطلاق الرموز.

### 3. تشغيل خادم المقاصة والـ API المحلي
لتشغيل خادم Node.js الرئيسي واستقبال طلبات حزم البيانات (Payloads) بالثانية الواحدة، نفذ:
```bash
node server/app.js
```

### 4. تشغيل خوارزمية الذكاء الاصطناعي لحساب المقادير الصحيحة
في نافذة طرفية أخرى، قم بتشغيل محرك البايثون للتأكد من مطابقة دقة الـ 7 خانات لـ Pi والـ 10 خانات لـ YER:
```bash
python ai-predictive-engine/ratio_calculator.py
```

### 5. تشغيل اختبارات المقاصة ومنع الإنفاق المزدوج
للتحقق من سلامة العقد الذكي وقفل الـ Anti-Double-Dipping التزامني، نفذ أمر الفحص:
```bash
npx hardhat test tests/payload_injector.test.js
```
